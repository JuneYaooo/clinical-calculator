# 贡献指南

感谢你帮助 Clinical Calculator 变得更可靠。这个仓库优先接受可追溯、可复现、边界清晰的改进。

## 可以贡献什么

- 修正计算器元数据、名称、别名、单位或来源链接。
- 为已有实现补充已知答案、边界案例和无效输入测试。
- 在来源充分时实现 `source_candidate` 条目。
- 改进 CLI、自然语言检索、文档和 Agent 工作流。
- 报告公式版本、人群适用范围或内容权利方面的问题。

## 本地验证

项目运行时只依赖 Python 标准库，测试需要 `pytest`：

```bash
python3 -m pip install pytest
python3 scripts/clinical_calculator.py validate
python3 -m pytest -q
```

## 医学计算器变更清单

提交新增或修改的计算逻辑时，请同时提供：

1. 具体的一手文献、官方指南或专业协会来源链接。
2. 公式、评分规则或参考表对应的版本/年份。
3. 输入变量、单位、上下限、缺失值处理和取整规则。
4. 适用人群、排除人群和关键临床限制。
5. 至少两个来源可核对的已知答案，以及边界和无效输入测试。
6. 对内容授权状态的说明；不要提交受保护的问卷题干或专有表格。

不要根据计算器名称、二手摘要或记忆补写缺失系数、阈值和表格单元格。证据不足时，应保留为回源候选并明确记录缺口。

## Pull request 建议

- 保持一次提交聚焦一个主题，避免同时做无关的批量格式化。
- 在描述中列出来源、版本、测试命令和测试结果。
- 若变更会影响既有 ID、输入名或输出结构，请明确说明兼容性影响。
- 更新目录数字或报告前，确认它们由当前注册表结果支持。

医学公式通过自动化测试只代表实现可复现，不代表已经获得临床发布批准。
